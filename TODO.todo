Azalea CLI:
-----------

    ✔ Separate the one behemoth `cli.py` into an actuall project structure but ngl I'm lowk lazy @started(26-02-28 16:33) @done(26-02-28 19:57) @lasted(3h24m50s)

    Features:
        ✔ Add `azalea update` which will update all the content @started(26-02-16 21:57) @done(26-02-16 22:03) @lasted(6m3s)
        ✔ Add `azalea upgrade` which will update the packs MC and fabric versions @started(26-02-16 13:12) @done(26-02-16 13:38) @lasted(26m34s)
        ✔ Add `azalea install -f file.txt` to install all mods from a file @started(26-02-16 15:42) @done(26-02-16 16:39) @lasted(57m10s)
        ✘ Add `azalea list` with the option of a Markdown table format for README's with links and HTML tag detection @cancelled(26-02-16 18:37)
        ✔ Add `azalea readme` ^ but renamed @started(26-02-16 18:20) @done(26-02-16 18:38) @lasted(18m14s)
        ✔ Add `azalea update -f` for forced updates meaning all mods get updated even if already latest (nice to have when updating jsons, because they'll get rebuild) @started(26-02-17 18:30) @done(26-02-17 18:45) @lasted(15m11s)
        ✔ Add `azalea search <query>` to search Modrinth from the CLI and display results @done(26-03-05)
        ✔ Add `azalea info <slug>` to display details of an installed mod (version, side, dependencies) @done(26-03-05)
        ✔ Add `azalea pin <slug>` / `azalea unpin <slug>` to lock a mod to its current version and skip it during `update` @done(26-03-05)
        ✔ Add `azalea remove -f file.txt` to batch-remove mods by analogy with `add -f` @done(26-03-05)
        ✔ `azalea check` without an argument should default to the current pack version (sanity check) @done(26-03-05)

    Bugs:
        ✔ Fix exporting to .mrpack (mc and fabric versions missing in header) @done(26-02-16 11:45)
        ✔ Fix server/client/both side (currently all `null` in the `json`) @started(26-02-16 11:45) @done(26-02-16 12:53) @lasted(1h8m20s)
        ✔ Do not make directories (shaders, datapacks) until they are needed @started(26-02-16 18:00) @done(26-02-16 18:18) @lasted(18m23s)
        ✔ CRITICAL: Modrinth API uploads fail, because my mod info does not inclide SHA1 @started(26-02-17 18:05) @done(26-02-17 18:46) @lasted(41m30s)
        ✔ Make azalea able to install resource packs that are not matching with target version with a warning @done(26-03-05)
        ✔ `azalea remove` only looks in mods/, not resourcepacks/ or shaderpacks/ @done(26-03-05)
        ✔ `fileSize` is hardcoded to 0 in exported .mrpack — should use the real size from the Modrinth API @done(26-03-05)
        ✔ `readme` uses project_id in Modrinth URLs instead of slug (both work, slug is cleaner) @done(26-03-05)
        ✔ Duplicate compatibility-check logic between `check` and `upgrade` — extract to shared helper @done(26-03-05)

    In progress:
        ✔ Fix `azalea init` crash on MC version picker (`KeyError: 'date_published'` — Modrinth game-version tags don't include that field) @done(26-03-05)
        ✔ Redesign `azalea init` UI: cleaner header, aligned labels, section dividers, compact multi-column MC version grid, inline loader row @done(26-03-05)

    Long-term goals:
        ✔ Support other modloaders than fabric @done(26-03-05)
        ✔ Fix shader loaders when downloading shaders (currently hardcoded options that don't check if required mod is installed, ex. iris, optifine) @done(26-03-05)
        ✔ Improve the `azalea init` commands, prompts user for name, defaults to `0.1.0` version, asks for author name, prints a searchable list of MC versions, selects a loader and uses the latest version (all in a order with the most sence) @done(26-03-05)

    Someday:
        - Publish package to PyPI
        - Add a CHANGELOG and GitHub release workflow


Miscellaneous:
--------------

    Repository:
        ✔ Make a proper README @started(26-02-28 20:05) @done(26-02-28 20:43) @lasted(38m58s)
        ✔ Add a formatting action @started(26-02-28 19:45) @done(26-02-28 20:30) @lasted(45m36s)
        ✔ Add a test action (connected with @Tests) @done(26-03-05)

    Formatting:
        ✔ Add a python formatter or linter @started(26-02-28 19:45) @done(26-02-28 20:30) @lasted(45m30s)
        ✘ Configure black formatter @cancelled(26-02-28 19:45)

    Tests:
        ✔ Implement tests, add a quick set of commands testing the CLI (done manually rn) @done(26-03-05)
        ✔ Mock the Modrinth API for unit tests (avoid real network calls) @done(26-03-05)
        - Add integration tests against the real Modrinth API (read-only, rate-limited)
